language: python

python:
- 3.6
- 3.7

dist: xenial

sudo: true

addons:
  apt_packages:
  - libcups2-dev
services:
- mysql
install:
- pip install --upgrade pip
- pip install flake8 edc-selenium
- pip install coveralls
- pip install -e .
- pip install -q -r requirements/stable.txt
before_script:
- flake8 ambition
- mysql -e 'create database edc character set utf8;'
script:
- coverage run --source=ambition manage.py test
after_success:
- coveralls
